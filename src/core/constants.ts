/** Prefix for auto-generated slug IDs (headings without explicit {#id}) */
export const AUTO_SLUG_PREFIX = "_auto:"

/** Node ID used for group labels (heading text in markdown) */
export const LABEL_NODE_ID = "_label"

/** Current manifest schema version */
export const MANIFEST_VERSION = 1
